{
  "name": "coding-interviews",
  "version": "1.0.0",
  "description": "Online coding interview platform with collaborative editing",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd server && uv run python run.py",
    "dev:frontend": "cd client && npm start",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd server && uv run pytest -v",
    "test:frontend": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
